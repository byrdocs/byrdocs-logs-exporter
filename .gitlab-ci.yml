image: node:22

stages:
  - check

typecheck:
  stage: check
  cache:
    key:
      files:
        - package-lock.json
      prefix: ${CI_JOB_NAME}
    paths:
      - node_modules/
    policy: pull-push
  script:
    - npm ci
    - npm run typecheck
